import { background } from "./colors/background";
import { border } from "./colors/border";
import { data } from "./colors/data";
import { fill } from "./colors/fill";
import { brand, critical, decorative, info, magic, marketing, neutral, primary, success, transparent, warning, } from "./colors/foundation";
import { icon } from "./colors/icon";
import { surface } from "./colors/surface";
import { text } from "./colors/text";
export const colors = {
    brand,
    marketing,
    success,
    critical,
    info,
    warning,
    decorative,
    magic,
    neutral,
    transparent,
    background,
    surface,
    fill,
    text,
    border,
    icon,
    data,
    /** @deprecated in favour of success or brand */
    primary,
};
