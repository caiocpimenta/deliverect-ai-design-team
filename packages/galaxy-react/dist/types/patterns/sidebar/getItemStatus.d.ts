import { SidebarButtonVariants } from '@deliverect/galaxy-styles';
/** Helper function to determine which status the button has */
export declare const getItemStatus: (active: boolean, isAvailable: boolean) => SidebarButtonVariants["status"];
